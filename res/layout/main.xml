<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="horizontal"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@drawable/gradients"
        >

    <LinearLayout
            android:orientation="vertical"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            >
        <ImageView
                android:layout_height="200dp"
                android:layout_width="fill_parent"
                android:src="@drawable/appglass2"
                />
        <TextView android:layout_width="fill_parent"
                  android:id="@+id/colorText"
                  android:layout_height="wrap_content"
                  android:textSize="25px"
                  android:gravity="center"
                  android:visibility="visible"/>
        <Button android:text="@string/btnIntendS" android:id="@+id/btnIntendS"
                android:layout_height="wrap_content"
                android:layout_width="200dp"
                android:layout_gravity="center"
                android:background="@drawable/buttons"
                android:drawableLeft="@drawable/camera"
                />
        <Button android:text="@string/howToBtn" android:id="@+id/howToBtn"
                android:layout_height="wrap_content"
                android:layout_width="200dp"
                android:layout_gravity="center"
                android:background="@drawable/buttons"
                android:drawableLeft="@drawable/help"
                />
        <Button android:text="@string/nextBtn" android:id="@+id/nextBtn"
                android:layout_height="wrap_content"
                android:layout_width="200dp"
                android:layout_gravity="center"
                android:background="@drawable/buttons"
                android:drawableLeft="@drawable/next"
                />
    </LinearLayout>
</LinearLayout>
